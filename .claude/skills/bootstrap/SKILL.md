Fully bootstrap this project with documentation, git setup, and initial commit.

## Steps

1. Read the codebase — scan structure, build files, test projects, key source files
2. Create/Update CLAUDE.md with: build commands, architecture overview, key conventions, testing instructions
3. Create .gitignore if missing (appropriate for the tech stack detected)
4. Git init if not already a repo (`git init`)
5. Stage all relevant files (exclude bin/, obj/, .env, secrets)
6. Create initial commit with message: `chore: initial project setup`
7. If a remote URL is provided, add it and push: `git remote add origin <url> && git push -u origin main`
8. Report: files committed, remote status, CLAUDE.md sections created
